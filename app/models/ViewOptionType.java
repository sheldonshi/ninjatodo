package models;

/**
 * the order of these enum values are important because their ordinal is tied to position of bit in view option
 */
public enum ViewOptionType {
    SORT, SHOW_COMPLETED, EXPAND_NOTES
}
